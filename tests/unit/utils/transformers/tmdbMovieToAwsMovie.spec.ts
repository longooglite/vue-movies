import { describe, it } from 'vitest'

describe('searchResultToAwsMovie (TMDB variant)', () => {
  it.todo('maps title, year via releaseDateToYear, director, id string, poster path')
  it.todo('handles missing optional fields gracefully')
})


