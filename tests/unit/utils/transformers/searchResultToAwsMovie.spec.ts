import { describe, it } from 'vitest'

describe('searchResultToAWSMovie', () => {
  it.todo('maps fields correctly and omits undefined values')
  it.todo('uses director.name when present, otherwise empty string')
  it.todo('converts id to string and year to number')
})


