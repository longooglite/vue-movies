import { describe, it } from 'vitest'

describe('stringValidator', () => {
  it.todo('returns false when movie field is missing or empty')
  it.todo('matches case-insensitively when substring is present')
  it.todo('returns false when substring is not present')
})


