import { describe, it } from 'vitest'

describe('multiSelectValidator', () => {
  it.todo('returns false when movie field is missing')
  it.todo('returns true when movie field (array) overlaps selections')
  it.todo('returns true when movie field (scalar) is included in selections')
  it.todo('returns false when there is no overlap with selections')
})


