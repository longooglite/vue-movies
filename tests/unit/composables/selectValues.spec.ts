import { describe, it } from 'vitest'

describe('selectValues composable', () => {
  it.todo('countryOptions returns unique, sorted countries from movies')
  it.todo('formatOptions returns unique, sorted formats from movies')
  it.todo('labelOptions returns unique, sorted labels from movies')
  it.todo('genreOptions returns unique, sorted genres from movies')
  it.todo('selectFormat sets the provided ref to the new format')
  it.todo('selectLabel sets the provided ref to the new label')
  it.todo('selectGenre sets the provided ref to the new genre')
  it.todo('selectCountry sets the provided ref to the new country')
})


