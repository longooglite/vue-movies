import { describe, it } from 'vitest'

describe('drawerStore', () => {
  it.todo('openDrawer sets currentDrawerId and opens the drawer')
  it.todo('closeDrawer clears currentDrawerId and closes the drawer')
})


